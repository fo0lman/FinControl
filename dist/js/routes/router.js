/**
 * Created by foolman on 30.07.2015.
 */
define([
    'backbone'
], function (Backbone) {
    "use strict";

    var Router;
    Router = Backbone.Router.extend({
        routes: {}
    });
    return Router;
});